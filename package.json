{
  "name": "shop",
  "private": true,
  "scripts": {
    "start": "prpl-server --root build/ --host 0.0.0.0 --https-redirect --bot-proxy https://render-tron.appspot.com/render",
    "build": "polymer build && node scripts/add_entrypoint_push.js",
    "deploy": "gcloud app deploy --project polymer-shop"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "dependencies": {
    "prpl-server": "^1.0.0"
  }
}
